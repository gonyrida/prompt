# Env for DevResourceHub backend
# Copy this file to .env and fill secrets
RESOURCE_API_KEY=
# Optional: point to an external resource provider endpoint that accepts the query params used by the server
# RESOURCE_API_URL=https://api.example.com/resources
PORT=3001

# Required for Chatbot endpoint (/api/chat)
GEMINI_API_KEY=
# Optional: if true, chatbot will return a mock response when the model API fails
CHATBOT_MOCK_ON_FAILURE=false
